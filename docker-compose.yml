services:
  mytools:
    build: .
    image: mytools:local
    container_name: mytools
    ports:
      - "${HOST_PORT:-9000}:${PORT:-8000}"
    environment:
      HOST: 0.0.0.0
      PORT: ${PORT:-8000}
    volumes:
      - ./:/app
    restart: unless-stopped
